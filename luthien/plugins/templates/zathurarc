set recolor "true"

set completion-bg "{{ colors.background | hex }}"
set completion-fg "{{ colors.foreground | hex }}"
set completion-group-bg "{{ colors.background | hex }}"
set completion-group-fg "{{ colors.accents.1 | hex }}"
set completion-highlight-bg "{{ colors.foreground | hex }}"
set completion-highlight-fg "{{ colors.background | hex }}"

set recolor-lightcolor "{{ colors.background | hex }}"
set recolor-darkcolor "{{ colors.foreground | hex }}"
set default-bg "{{ colors.background | hex }}"

set inputbar-bg "{{ colors.background | hex }}"
set inputbar-fg "{{ colors.foreground | hex }}"
set notification-bg "{{ colors.background | hex }}"
set notification-fg "{{ colors.foreground | hex }}"
set notification-error-bg "{{ colors.accents.0 | hex }}"
set notification-error-fg "{{ colors.foreground | hex }}"
set notification-warning-bg "{{ colors.accents.0 | hex }}"
set notification-warning-fg "{{ colors.foreground | hex }}"
set statusbar-bg "{{ colors.background | hex }}"
set statusbar-fg "{{ colors.foreground | hex }}"
set index-bg "{{ colors.background | hex }}"
set index-fg "{{ colors.foreground | hex }}"
set index-active-bg "{{ colors.foreground | hex }}"
set index-active-fg "{{ colors.background | hex }}"
set render-loading-bg "{{ colors.background | hex }}"
set render-loading-fg "{{ colors.foreground | hex }}"

set window-title-home-tilde true
set statusbar-basename true
set selection-clipboard clipboard
